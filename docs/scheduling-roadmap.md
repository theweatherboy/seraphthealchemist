# Scheduling roadmap

The current request queue supports preferred availability, manual scheduling, notes, and status changes. Future scheduling work should add an admin availability editor for recurring hours, date overrides, and blocked periods; conflict checks that prevent overlapping appointments; capacity limits by service type across each day, week, and month; customer notifications; and optional calendar synchronization.
